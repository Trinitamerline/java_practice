import java.util.*;
class Linked_List
{
	public static void main (String [] args)
	{
		LinkedList<Integer> a = new LinkedList<>();
		/*

		To add elements in the list.
		a.add(12);
		
		To add elements at the specified index
		a.add(3,15);

		To add elements at first and last
		a.addFirst(15);
		a.addLast(89);

		To clear the elements in the list.
		a.clear();

		To clone the elements into another list
		ArrayList b = new ArrayList();
		b = (ArrayList)a.clone();
		System.out.println(a+" "+b);

		To get size of the List
		System.out.println(a.size());	

		To check whether the element is in the list or not
		System.out.println(a.contains(15));

		To retrive element but does not remove the head
		System.out.println(a.element(1));	

		To retrieve elements in the list
		System.out.println(a.get(0));

		To retrieve elements at the first and last
		System.out.println(a.getFirst());	
		System.out.println(a.getLast());	

		To get the first occurence of the given element
		System.out.println(a.indexOf(0)); //lastIndexOf

		To add element at the tail of the list
		a.offer(12);

		To add element at first and last of the list
		a.offerFirst(13);
		a.offerLast(14);

		To retrive without removing the first element(head)
		a.peek();

		To retrieve first and last elements without removing the element , returns null when the list is empty
		a.peekFirst();
		a.peekLast();

		To retrieve and remove the first element
		a.poll();

		To retrieve and remove the first and last elements
		a.pollFirst();
		a.pollLast();

		To remove elements from the stack
		a.pop();

		To add elements in the stack
		a.push();

		To remove an particular element in the list
		System.out.println(a.remove(0));

		To return the element and remove the element .
		System.out.println(a.removeFirst());
		System.out.println(a.removeLast());

		To delete the first and last occurences of the list
		a.removeFirstOccurrence();
		a.removeLastOccurrence();

		To change particular value at the index
		System.out.println(a.set(0 , 13)); index,element

		Spliterator,descendingiterator,toarray

		*/
	}
}
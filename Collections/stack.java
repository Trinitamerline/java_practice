import java.util.*;
class Stack_
{
	public static void main (String [] args)
	{
		Stack<Integer> a = new Stack<>();
		/*
		To add elements in the stack
		a.push();

		To retrieve and remove the element in the stack
		a.pop();

		To retrieve the top element of the stack
		a.peek();

		To check whether stack is empty or not
		a.empty();

		To search the availability of the element in the stack
		a.search();         +1 => available  -1 => not available

		*/
	}
}
